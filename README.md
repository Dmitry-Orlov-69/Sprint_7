# В проекте есть папка с классами для тестирования соответствующих ручек, и папка с тестами, внутри которой 4 папки, соответствующих тестированию каждой из ручек. Названия тестов указаны внутри каждого файла с тестом.